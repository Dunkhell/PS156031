#include <stdio.h>
#include <stdlib.h>
int foo (int *a, int *b)
{
    if (*a > *b)
        return *b;
    else return *a;
}
int main()
{
    int a,b;
    scanf("%i",&a);
    scanf("%i",&b);
    printf("Mniejsza liczba jest %i",foo(&a,&b));
    return 0;
}
