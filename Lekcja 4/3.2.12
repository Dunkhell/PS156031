#include <stdio.h>
#include <stdlib.h>
int foo (unsigned int n)
{
    double *tablica;
    tablica = (double*) malloc(n * sizeof(*tablica));
    return tablica;
}
int main()
{
    int *a= foo(1);
    printf("%d",a);
    return 0;
}
