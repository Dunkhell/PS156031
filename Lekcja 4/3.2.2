#include <stdio.h>
#include <stdlib.h>
int* foo(int *a, int *b)
{
    if(*a<*b)
        return a;
    else
        return b;
}
int main()
{
    int x=10,y=5,z;
    z= foo(&x,&y);
    printf("%d",&z);
    return 0;
}
