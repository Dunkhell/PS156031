#include <stdio.h>
#include <stdlib.h>
int foo (int tab[][100][100])
{
    int suma=0;
    for (int i=0;i<100;i++)
        for (int j=0;j<100;j++)
            for (int o=0;o<100;o++)
                suma+=tab[i][j][o];
        return suma;
}
int main()
{
    return 0;
}
